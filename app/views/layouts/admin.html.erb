<!DOCTYPE html>
<html class="h-100" lang="<%= locale %>">
  <head>
    <%= render 'shared/head' %>
  </head>

  <body class="bg-light">
    <div id="toasts" class="position-absolute top-0 end-0"></div>

    <div class="admin container-fluid bg-light">
      <div class="row flex-nowrap">
        <%= render 'shared/admin_navbar' %>

        <main class="main col pt-4">
          <%= render "shared/admin_header" %>
          <%= yield %>

          <div class="card m-4 p-2">
            <%= debug(params) if Rails.env.development? %>
          </div>
        </main>
      </div>
    </div>
  </body>
</html>
