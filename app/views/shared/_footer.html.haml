%footer.bg-light.footer.mt-auto.py-4.border-top
  .container
    .row.mb-3
      .col-12.col-md-4
        = render(Footer::ContactUsComponent.new)
      .col-12.col-md-4
        = render(Footer::EventsComponent.new(events: @events))
    .row
      .col-12.text-center
        © #{Date.today.year} #{t('community_title')}
